{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Bot",
      "program": "${workspaceFolder}/src/index.ts",
      "preLaunchTask": "TSC",
      "envFile": "${workspaceFolder}/.env",
      "outFiles": [
        "${workspaceFolder}/build/**/*.js"
      ]
    }
  ]
}